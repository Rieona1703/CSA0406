#include <stdio.h>

int buffer[5], front = 0, rear = -1;

int main() {
    int i;

    // Producer
    for(i = 1; i <= 5; i++)
        buffer[++rear] = i;

    // Consumer
    printf("Buffer: ");
    while(front <= rear)
        printf("%d ", buffer[front++]);

    return 0;
}
