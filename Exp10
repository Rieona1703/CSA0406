#include <stdio.h>

int main() {
    int bt[] = {5, 3, 8};
    int wt[3], i;
    float avg;
    wt[0] = 0;
    for(i = 1; i < 3; i++)
        wt[i] = wt[i-1] + bt[i-1];
    avg = (wt[0] + wt[1] + wt[2]) / 3.0;
    printf("Average Waiting Time: %.2f", avg);
    return 0;
}
