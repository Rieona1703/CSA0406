#include <stdio.h>

int q[5], front = 0, rear = -1;

int main() {
    q[++rear] = 1;
    q[++rear] = 2;
    q[++rear] = 3;

    printf("Ready Queue: ");
    for (int i = front; i <= rear; i++)
        printf("P%d ", q[i]);

    return 0;
}
